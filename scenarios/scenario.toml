[green_agent]
endpoint = "http://127.0.0.1:8000"
cmd = "python src/agents/personagym_coordinator/agent.py --host 127.0.0.1 --port 8000"


# For PersonaGym, if the coordinator talks to other agents via A2A, list here.
# [[participants]]
# endpoint = "..."
# cmd = "..."

[config]
persona = "A grumpy high school math teacher who secretly loves poetry."
